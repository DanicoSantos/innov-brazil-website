jQuery(document).ready(function ($) {
  $("#partners").owlCarousel({
    loop: true,
    // smartSpeed: 450,
    margin: 30,
    nav: false,
    items: 5,
    itemElement: "div",
    // navElement: "span",
    // dots: true,
  });
});
